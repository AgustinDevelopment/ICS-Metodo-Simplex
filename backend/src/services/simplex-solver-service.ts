import { SimplexProblem, ObjectiveFunction, Constraint } from '../types/types';

export class SimplexSolverService {
  validateProblem(problem: SimplexProblem): boolean {
    // ...código de validación...
    return true; // Cambia esto según la lógica de validación real
  }

  createSimplexTableau(problem: SimplexProblem) {
    // ...código del tableau...
  }
}
